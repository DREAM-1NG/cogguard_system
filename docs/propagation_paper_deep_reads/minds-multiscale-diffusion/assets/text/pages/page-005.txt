The Thirty-Eighth AAAI Conference on Artiﬁcial Intelligence (AAAI-24)

LSTM to encode different aspects of the inputs. The orthog-            Dataset     Christ   Android  Douban  Meme
onality constraints are defined as:                                                               # Users        2,897     9,958     12,232     4,709
                            2                      2                # Links       35,624    48,573     39,658    209,194
  Ldiff =  HshareT Hcas  +  HshareT Huser     ,  (10)         # Cascades     589      679       3,475     12,661
                      F                 F
                                                                  Avg. Length    22.9      33.3      21.76     16.24
where ∥·∥2F is the squared Frobenius norm.
                                                             Table 1: Statistics of datasets. Christ is short for the dataset
Diffusion Prediction                                              Christianity, and Meme is short for the dataset Memetracker.
We concatenate the task-specific representation Hcas and
Huser for each task with the shared representation Hshare,
                                                             Baselines  We compare  thirteen representative baselinerespectively. These concatenated representations are then
                                                       models with our models.separately fed into distinct output layers dedicated to the pre-
                                                            For macroscopic prediction, we evaluate five models:diction process.
                                                    DeepCas (Li et al. 2016), DeepHawkes (Cao et al. 2017),
Macroscopic Diffusion Prediction  For macroscopic dif-     CasCN (Chen et al. 2019b), CasFlow (Xu et al. 2023b) and
fusion prediction, we aim to predict the final cascade size in     TCSE-net (Wu et al. 2022).
the future. We calculate the final size of diffusion cascade        For microscopic  prediction, we evaluate  six models:
dm by:                                         TopoLSTM (Wang et al. 2017), NDM (Yang et al. 2021),
                                            SNIDSA (Wang, Chen, and Li 2018), Inf-VAE (Sankar et al.
       Sm = MLP(concat(hcas, hshare)),       (11)                                                              2020), DyHGCN (Yuan et al. 2020) and TAN-DRUD (Liu
where concat(·, ·) is the concatenation operation.                   et al. 2022).
  We train the macroscopic task by minimizing the follow-        For multi-scale prediction, we evaluate two models: FOR-
ing loss function:                                 EST (Yang et al. 2019) and DMT-LIC (Chen et al. 2019a).
                                      A detailed description of baselines can be found in the
               M
                    1                                  Appendix.
           Lmacro = X (Sm −ˆSm)2,         (12)
          M                    m=1                             Evaluate Metrics  For macroscopic prediction, we use
                                               Mean Squared Logarithmic Error (MSLE) as the evalua-
where M is the number of diffusion cascades and ˆSm is the       tion metric, which is also used in previous experiments (Cao
ground truth.                                                           et al. 2017; Li et al. 2016). For microscopic prediction, we
                                                            use two ranking metrics used in (Yang et al. 2019): MeanMicroscopic Diffusion Prediction  For microscopic diffu-
                                                          Average Precision on top k (MAP@k) and Hits Scores onsion prediction, we predict the next influenced probability
                                                               top k (Hits@k) for evaluation, k = [10, 50, 100].pi ∈R|dm| for user ui:
                                                      Parameters Settings  For each dataset, we employ a ran-
     pi = softmax(MLP(concat(huser, hshare))).    (13)                                             dom sampling method to allocate 80% of cascades for train-
We adopt the cross entropy loss for microscopic training:          ing, 10% for validation, and the remaining 10% for test-
                                                                      ing. Baseline methods follow the original paper settings. For
                           |dm| |U|                       MINDS, we implement the model using PyTorch and uti-
           Lmicro = − X X ˆpji log(pji),         (14)        lize the Adam optimizer with a learning rate of 0.001. The
                       j=2 i=1                           embedding dimension is set to 64, and the batch size is 32.
                                                   The balance parameter λ is assigned a value of 0.3, whilewhere |U| is the number of users and ˜p is true probability. If
                                                                 the hyperparameter γ is set to 0.05. Social homophily learn-user ui participate in cascade dm at the step j, then ˆpji = 1,
                                                               ing utilizes a 2-layer GCN, and global interaction learning isotherwise ˆpji = 0.
                                                                         facilitated through a single-layer HGNN. Additionally, the  The overall loss function of our model is defined as:
                                                    number of time intervals is set to 8.
 L = λLmacro + (1 −λ)Lmicro + Ladv + γLdiff,  (15)
                                                 Performance Comparison
where λ is a balance parameter and γ is a hyperparameter.
                                         We conduct a comprehensive comparison of MINDS with
                                                                various baselines on four datasets, focusing on microscopic
               Experiment                                                       and macroscopic diffusion prediction. The results are sum-
In this section, we conduct experiments on both microscopic      marized in Tables 2, 3, and 4, and we observe the following:
and macroscopic cascade predictions to demonstrate the ef-         1) MINDS consistently outperforms all state-of-the-art
fectiveness of our proposed model.                               baselines in microscopic prediction tasks. Compared to the
                                                              second-best model DyHGCN, MINDS leverages sequential
Experimental Setting                                     hypergraphs to dynamically represent cascade interactions,
Datasets  We conduct experiments on four datasets, i.e.,      leading to remarkable improvements of up to 3% in Hits
Christianity, Android, Douban and Memetracker. The statis-       scores and MAP scores.
tics of these datasets are shown in Table 1. A detailed de-         2) MINDS consistently outperforms all state-of-the-art
scription of the datasets can be found in the Appendix.            baselines in macroscopic prediction tasks, achieving at least





                                                   8575
