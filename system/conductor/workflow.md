# Workflow

- Preserve user-owned changes in the dirty worktree.
- Add a focused contract test for a user-facing visualization change.
- Validate affected frontend code with `npm.cmd test`, `npx vue-tsc -b --pretty false`, and `npm.cmd run build` when practical.
- Update the active track when the implementation and validation finish.
