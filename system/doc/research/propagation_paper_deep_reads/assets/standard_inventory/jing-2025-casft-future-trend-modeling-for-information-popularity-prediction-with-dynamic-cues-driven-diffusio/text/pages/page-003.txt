Cascade Graph: Given a tweet I and an observation time
to, the cascade graph can be defined as G(to) = (Vc, Ec),                min Eq(x0) ≤min Eq(x0:K)L             (6)
where Vc represents the set of users involved in C(to), and                         θ              θ
Ec represents the set of edges, each denoting a retweet rela-                      K     pθ(xk−1|xk)tionship between two users (nodes).                       L = −log p(xK) − X log                     (7)
                                                                                         q(xk|xk−1)Cascade Sequence: Different from the cascade graph, the                              k=1
cascade sequence S(to) for cascade C(to) is a collection of
users, each arranged in chronological order based on their             Our Method: CasFT
timestamps, i.e., S(to) = {u0, u1, . . . , uN}.                 Our proposed CasFT can be summarized as a three-step
Global Graph: Given all the retweet cascades under the      model: the first step involves extracting spatiotemporal pat-
observation time to, we define the global graph as G =       terns from the observed cascade, the second step simulates
{GI(to)|I ∈I}, where the edge in Eg represents the node       the future trends of popularity increment, and the third step
relationship from cascading, such as the follower/followee     makes the final prediction. The overall framework is shown
relationship in the social network.                                  in Fig. 2. We discuss the key components below.
Cascade Popularity Prediction: Given a cascade C(to) and
a duration to, we predict its incremental popularity P over      Historical Spatiotemporal Modeling
the time interval from to to tp , where tp ≫to, and tp is the      Given an observed cascade C(t0), CasFT first extracts the
prediction time. P represents the number of triplets occur-      spatiotemporal features from  it. Specifically, this step in-
ring during the time period (to, tp).                             volves two components: (1) spatial structural pattern extrac-
                                                                     tion from the observed cascade graph G(t0) and the global
Neural Ordinary Differential Equations                  graph G, and (2) temporal pattern extraction based on the
A neural Ordinary Differential Equation (ODE) (Chen et al.      cascade sequence. The details are outlined as follows.
2018) describes the continuous-time evolution of variables.                                                              Spatial Structural Pattern Extraction:  In light of the
It represents a transformation of variables over time, where                                                                        distinct  characteristics between  the cascade and global
the initial state at time t0, denoted as h(t0), is integrated       graphs, we employ two different graph embedding methods
forward using an ODE to determine the transformed state at                                                                     to extract unique structural patterns from each, respectively.
any subsequent time ti.                                      For the cascade graph, we use GraphWave (Donnat et al.
                                                         2018) to capture local structural information, as referenced
       dh(t)
        = f(h(t), t; θ)  where  h(0) = h0     (1)       in previous studies (Zhou et al. 2020b; Xu et al. 2021). For
         dt                                                     the large-scale global graph, we employ NetSMF (Qiu et al.
                       Z  ti dh(t)                       2019), which is designed for large-scale networks and can
              h(ti) = h(t0) +            dt,            (2)      quickly learn interactions between all users, efficiently em-
                                         t0   dt                                                          bedding these global structural patterns into node embed-
where f denotes a neural network, such as a feed-forward or       dings. Specifically, given the observed cascade graph G(to)
convolutional network.                                    and the global graph G, we calculate the local-view and
                                                              global-view representations of users as follows:
Diffusion Models
                                                             Ec = GraphWave(G(to)),              (8)
Diffusion models (Ho, Jain, and Abbeel 2020) are used to
generate high-quality samples from complex data distribu-                   Eg = NetSMF(G),                  (9)
tions through a bi-directional process.                        where Ec and Eg represent local-view and global-view rep-
  The forward process of diffusion model  is a Markov       resentations for users in G(to) and G, respectively. For a spe-
Chain that gradually adds Gaussian noise to x0:                      cific user ui, we retrieve its representations from these two
                                                          views according to its index, denoted as Ec(ui) and Eg(ui).
                          T
              q(x1:T |x0) = Y q(xt|xt−1),             (3)     Temporal Pattern Extraction:  In this phase, we split
                           t=1                                 the cascade sequence S(to) into multiple sub-sequences
                                                                     in chronological order, represented as S = {Sj}j∈N =
        q(xt|xt−1) = N(xt; p1 −βtxt−1, βtI),      (4)      {(u0), (u0, u1), . . . , (u0, u1, ..., uN)}, and employ the self-
                                                                     attention mechanism (Vaswani et al. 2017) to each sub-
where T is the timesteps of adding noise and βt ∈(0, 1) is                                                          sequence to capture its internal interdependencies, thereby
the variance schedule.                                                             accounting for the long-term history information. Firstly, for
  For the denoising process, the goal is to learn a condi-                                                          each timestamp ti, we define a temporal encoding procedure
tional probability distribution pθ(xt−1|xt) that reverses the                                                     by trigonometric functions:
diffusion process:
                                                                               B  ), if j is odd,                                           ( cos(ti/10000 j−1
      pθ(xt−1|xt) = N(xt−1; µθ(xt, t), σθ(xt, t)I),    (5)                 [z(ti)]j =                       j                   (10)
                                                                                sin(ti/10000 B ), if j is even.
where the parameter θ can be optimized by minimizing
the negative log-likelihood via an Evidence Lower Bound    We deterministically compute z(ti) ∈RB, where B is the
(ELBO):                                                  dimension of encoding and get Zt which is the set of z(ti).


                                                  11908
