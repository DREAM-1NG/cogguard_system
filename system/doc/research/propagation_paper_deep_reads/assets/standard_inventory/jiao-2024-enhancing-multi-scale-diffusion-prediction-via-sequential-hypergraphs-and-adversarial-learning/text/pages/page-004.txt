The Thirty-Eighth AAAI Conference on Artiﬁcial Intelligence (AAAI-24)

matrix of self-looped GS. The initial homophily embedding      hidden state, while the forget gate ft regulates the amount
matrix X0S ∈RN×d is randomly initialized from a normal       of information discarded from the previous memory cells ct.
distribution, and d is the dimension of embedding. We can       Additionally, the output gate ot determines the amount of in-
obtain the final social homophily representation XS after      formation to be output in the hidden state ht. By integrating
several layers of GCN.                                           the forget gate, input gate, update memory unit, and output
                                                                      gate, the shared LSTM can effectively handle the intricate
Shared-private Representation Learning                    relationship between micro-features and macro-features.
Graph-based  representation  learning  captures  the  co-      We  finally obtain a comprehensive representation of
occurrence relationship of users at the user level and cascade      shared  features between  the macro-prediction  task and
level, however, it does not enable further analysis of context      micro-prediction task from the shared LSTM, which is de-
interactions within cascades. Therefore, due to the excellent      noted as Hshare ∈RN×d.
performance of the LSTM in sequential tasks such as natural                                                          Adversarial Training  Although the shared-private LSTM
language processing, we apply two LSTM modules to learn                                                                                is designed to learn the shared and task-specific features,
the social and global context interactions within cascades,                                                                   there is no guarantee that shared features can not be pre-
respectively.                                                              served in private feature space, or vice versa. Therefore, a
  We hold the belief that there exists a hidden common fea-                                                            simple principle can be applied to shared LSTM that a re-
ture between the macro-prediction and the micro-prediction                                                                        liable shared feature should primarily consist of common
tasks, with the potential to enhance the performance of each                                                              information without any task-specific information. Inspired
task individually. Drawing inspiration from the principles of                                                     by adversarial networks, we introduce adversarial training to
multi-task learning (Liu, Qiu, and Huang 2017), we propose                                                                solve this problem.
a shared LSTM architecture aimed at capturing these shared                                      A task discriminator is used to map the representation
characteristics between the two tasks. Furthermore, to tackle                                                                     into a probability distribution, estimating which tasks the en-
the issue of feature redundancy, we introduce a combination                                                        coded feature comes from.
of adversarial training and orthogonality constraints.
                                                             D(h, θD) = softmax(b + Uh),           (8)
Private Representation Learning  We utilize the LSTM
to model the cascade diffusion process sequentially, where a      where U ∈Rd×d is a learnable parameter and b ∈Rd is a
hidden state is employed to capture the diffusion history.           bias.
  The update of each LSTM unit can be shortened as:            To prevent task-specific features from  infiltrating the
                                                             shared representation, we design a task adversarial loss, de-
              ht = LSTM(ht−1, xt, θp),             (5)      noted as Ladv. This loss function is employed to train the
where ht ∈Rd is the hidden state and xt ∈Rd is the input     model in such a way that the shared features generated are
                                                               not easily predictable by a classifier in terms of their corre-
at the current time step. θp represents all the parameters in
                                                          sponding tasks. Formally, the task adversarial loss, Ladv, isLSTM.
                                                               defined as follows:  Based on the defined LSTM, we can compute the repre-
sentation of the context interaction for the user social ho-                            2  N
mophily XS and user global interaction matrix XD as fol-      Ladv = min max X X (log D(hkn)+log(1−D(hsharen    ))),                                                                                 θshare θD
lows:                                                                   k=1 n=1
            hcast  = LSTM(hcast−1, xdt , θcas)                                                                                 (9)
                                                           (6)      where θshare represents all the parameters in Shared-LSTM,
           husert  = LSTM(husert−1 , xst, θuser),                                                       and k denotes the task type (either macro or micro). The op-
where LSTM(., θ) is defined as Eq. 5.                           timization process involves a min-max framework, with the
  Thus, the task-specific embeddings are represented as      underlying concept being that the shared LSTM generates
Hcas ∈RN×d and Huser ∈RN×d.                            a representation to intentionally confuse the task discrimi-
                                                                       nator. As the training progresses, the shared feature extrac-
Shared Representation Learning  Inspired by the gated
                                                                        tor and task discriminator gradually reach a point of con-
mechanisms used in LSTM, we design a novel shared-
                                                               vergence, beyond which achieving additional enhancements
LSTM, that takes XD and XS as the input. The detail of                                                     becomes challenging. As a result, the task discriminator be-
the module is described as follows:
                                                    comes progressively incapable of distinguishing among var-
      ft = σ(xDt−1Wf + xSt−1Uf + ht−1Vf + bf),               ious tasks. This convergence indicates the successful acqui-
                                                                        sition of shared feature generation by the feature extractor,        it = σ(xDt−1Wi + xSt−1Ui + ht−1Vi + bi),                                                                     resulting in shared features that exhibit indistinguishability
      ot = σ(xDt−1Wo + xSt−1Uo + ht−1Vo + bo),       (7)       across all tasks.
          ˜ct = tanh(xDt−1Wc + xSt−1Uc + ht−1Vc + bc),           Orthogonality Constraints   It  is worth noting that the
       ct = ˜ct · it + ct−1 · ft,     ht = ot · tanh ct,            above model has a potential disadvantage. The disadvantage
                                                                                is that task-invariant features can appear in both shared and
where σ is the sigmoid function. W∗∈Rd×d, U∗∈Rd×d,       private representations. To alleviate this drawback, we in-
V∗∈Rd×d and b∗∈Rd are trainable parameters. The input      troduce orthogonality constraints, which penalize redundant
gate it controls the amount of new information added to the       latent representations and encourage the shared and private





                                                   8574
